" When starting to edit a file:
"   For *.c,*.cpp and *.h files set formatting of comments and set 
"   C-indenting on, and don't use hardtabs!!
"   For other files, use hardtabs it off.
"   Don't change the order, it's important that the line with * comes first.
" set softtabstop=4 cindent formatoptions=croql 
""set cindent comments=sr:/*,mb:*,el:*/,://

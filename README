Installation:

    git clone git://github.com/olsonbg/dotvim.git ~/.vim

Update submodules:

    cd ~/.vim
    git submodule init
    git submodule update

Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc

To upgrage each bundled plugin:

    git submodule foreach git pull origin master



